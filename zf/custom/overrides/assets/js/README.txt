Note: overrides.js is NOT included by default for performance reasons. To enable this override access the template administration page, open the tab "Advanced Options" and set "Enable 'overrides.js'?" to "true".

Use this file to override any of the native functions of the zhongFramework object. To see all the available functions, inspect the object "window.zhongFramework" from the console.
